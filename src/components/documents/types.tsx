export type DocumentProperties = {
    documentName: string,
    documentURL: string,
    uid: string, // Assuming UID is a string, change as necessary
    uEmail: string,
    uName: string,
    uPhoto: string,
    fileType: string;
    fileSize: string;
  };